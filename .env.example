# Gemini API Key (get free from https://aistudio.google.com)
GEMINI_API_KEY=your_key_here

# Chrome profile path (auto-detected if left as 'auto')
CHROME_PROFILE_PATH=auto

# Server port
PORT=3000

# Browser visibility (true = hidden, false = visible)
HEADLESS=false
